<UserControl x:Class="Otchi.Video.VideoPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Otchi.Video"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             xmlns:video="clr-namespace:Otchi.Video"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="VideoPlayerView">
    <vlc:VideoView Name="VideoView"></vlc:VideoView>
    <!--<vlc:VideoView Name="VideoView">
    <Canvas Name="VideoCanvas"
                Width="{Binding ActualWidth, ElementName=VideoView}"
                Height="{Binding ActualHeight, ElementName=VideoView}"
                MouseMove="VideoView_OnMouseMove"
                Background="#01000000">
        <Grid Canvas.Bottom="0"
                  Height="50"
                  Width="{Binding ActualWidth, ElementName=VideoCanvas}"
                  Name="ControlPanel">
            <Grid.Background>
                <SolidColorBrush>Red</SolidColorBrush>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Click="Fullscreen_Clicked"
                        Margin="10, 10"
                        Grid.Column="0">
                FullScreen
            </Button>
            <Button Name="PauseToggle" 
                        Click="Pause_Toggle"
                        Margin="10, 10"
                        Grid.Column="1">
                Pause
            </Button>
            <Label Grid.Column="2">
                Huh
            </Label>
        </Grid>
    </Canvas>
    </vlc:VideoView>-->
</UserControl>
