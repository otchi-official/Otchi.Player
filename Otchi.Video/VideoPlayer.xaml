<UserControl x:Class="Otchi.Video.VideoPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Otchi.Video"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <vlc:VideoView Name="VideoView">
            <Grid Width="{Binding ActualWidth, ElementName=VideoView}"
                       Height="{Binding ActualHeight, ElementName=VideoView}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="1"
                            Height="50"
                            Orientation="Horizontal">
                    <StackPanel.Background>
                        <SolidColorBrush Color="BlueViolet" Opacity="0.5"></SolidColorBrush>
                    </StackPanel.Background>
                    <Button Click="Fullscreen_Clicked">
                        FullScreen
                    </Button>
                </StackPanel>
            </Grid>
        </vlc:VideoView>
    </Grid>
</UserControl>
